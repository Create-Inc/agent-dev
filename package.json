{
  "name": "agent-dev",
  "version": "0.1.0",
  "description": "Dev server process manager for AI agents",
  "type": "module",
  "bin": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "link": "npm run build && npm link",
    "test": "npm run build && node --test --experimental-strip-types test/e2e.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/yargs": "^17.0.35",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "yargs": "^18.0.0"
  }
}
